# Nur fuer die lokalen Datenbanktests (pnpm test:db).
# Bewusst getrennt von .env.example, damit Vercel diese Variable beim
# Projektimport nicht als Deployment-Variable vorschlaegt.
#
#   createdb brokertool_test
#   cp .env.test.example .env.test
#
TEST_DATABASE_URL=postgres://postgres@localhost:5432/brokertool_test
